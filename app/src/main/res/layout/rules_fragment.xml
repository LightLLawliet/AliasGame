<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="@dimen/margin_large"
    android:background="@color/white"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/margin_medium"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/back_button"
            android:src="@drawable/ic_arrow" />

        <TextView
            style="@style/fragmentTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/rulesTitleText" />
    </androidx.appcompat.widget.Toolbar>

    <TextView
        style="@style/subtitle_style"
        android:text="@string/game_goal"
        app:drawableStartCompat="@drawable/ic_cup" />

    <View style="@style/underline" />

    <TextView
        style="@style/description_style"
        android:text="@string/goal_game_text" />

    <TextView
        style="@style/subtitle_style"
        android:text="@string/principle_of_game_title"
        app:drawableStartCompat="@drawable/ic_game" />

    <View style="@style/underline" />

    <TextView
        style="@style/description_style"
        android:text="@string/principle_of_game_description" />

    <TextView
        style="@style/subtitle_style"
        android:text="@string/forbidden_subtitle"
        app:drawableStartCompat="@drawable/ic_forbidden" />

    <View style="@style/underline" />

    <TextView
        style="@style/description_style"
        android:text="@string/forbidden_description" />

</LinearLayout>